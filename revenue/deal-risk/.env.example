# Deal-Risk outreach config (copy to .env.local; never commit .env.local)
FIRM_NAME="Acuna Law"
FIRM_CONTACT="team@firm.com / (312) 555-0100"
FIRM_ADDRESS="123 W Example St, Suite 100, Chicago, IL 60601"   # physical address — required for live send (CAN-SPAM)
EMAIL_FROM="Acuna Law <noreply@firm.com>"                        # verified Resend sending domain
RESEND_API_KEY=""                                                # from Ben's Resend account
OPTOUT_EMAIL=""                                                  # defaults to EMAIL_FROM address
MLS_EXPORT_DIR="/Users/benm4mini/Documents/Projects/10_AcunaLaw/scraper/downloads"
SOCRATA_APP_TOKEN=""                                             # optional — raises open-data rate limits
DEALRISK_SEND="0"                                               # set to 1 to actually email in the daily run
